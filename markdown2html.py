#!/usr/bin/python3
" Starter script for the Markdown2Html "
from sys import argv

if __name__ == "__main__":
    if argv != 2:
        raise SyntaxError
    if argv[1] is not file:
        raise FileNotFoundError
